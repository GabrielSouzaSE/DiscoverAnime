import styled from 'styled-components';

const DescriptionDiscoverAnime =  styled.p`
    font-size: 18rem;
    color: var(--color-gray-light);
    font-weight: 300;
`;

export default DescriptionDiscoverAnime;